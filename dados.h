#ifndef DADOS_H
#define DADOS_H

/*
   Declarações antecipadas (forward declarations) das structs ListaR e ListaI.
   Isso permite que outros arquivos de cabeçalho as utilizem sem causar dependência circular.
*/
typedef struct ListaR ListaR;
typedef struct ListaI ListaI;

#endif
